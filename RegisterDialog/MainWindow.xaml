<Window x:Class="ChatClientWPF.RegisterDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Регистрация" Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Имя:" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox x:Name="tbName" Grid.Row="1" Height="30" Margin="0,0,0,10" Padding="5"/>

        <TextBlock Grid.Row="2" Text="Логин:" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox x:Name="tbLogin" Grid.Row="3" Height="30" Margin="0,0,0,10" Padding="5"/>

        <TextBlock Grid.Row="4" Text="Пароль:" FontWeight="Bold" Margin="0,0,0,5"/>
        <PasswordBox x:Name="tbPassword" Grid.Row="5" Height="30" Margin="0,0,0,20" Padding="5"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Click="BtnCancel_Click" Margin="0,0,10,0" 
                    Padding="15,5" Width="100"/>
            <Button Content="Зарегистрироваться" Click="BtnRegister_Click" 
                    Padding="15,5" Width="150" Background="#4ecdc4"/>
        </StackPanel>
    </Grid>
</Window>