<Window x:Class="ChatClientWPF.SendMessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Отправить сообщение" Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="ID чата:" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox x:Name="tbChatId" Grid.Row="1" Height="30" Margin="0,0,0,10" Padding="5"/>

        <TextBlock Grid.Row="2" Text="Сообщение:" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox x:Name="tbMessage" Grid.Row="3" AcceptsReturn="True" 
                 TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                 MinHeight="100" Margin="0,0,0,20" Padding="5"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Click="BtnCancel_Click" 
                    Margin="0,0,10,0" Padding="15,5" Width="80"/>
            <Button Content="Отправить" Click="BtnSend_Click" 
                    Padding="15,5" Width="80" Background="#4ecdc4"/>
        </StackPanel>
    </Grid>
</Window>