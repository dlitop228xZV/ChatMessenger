<Window x:Class="ChatClientWPF.CreateChatDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.comwinfx/2006/xaml"
        Title="Создать чат" Height="250" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Название чата:" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox x:Name="tbName" Grid.Row="1" Height="30" Margin="0,0,0,10" Padding="5"/>

        <CheckBox x:Name="cbIsGroup" Grid.Row="2" Content="Групповой чат" 
                  Margin="0,0,0,10" FontWeight="Bold"/>

        <TextBlock Grid.Row="3" Text="Участники (ID через запятую):" 
                   FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox x:Name="tbParticipants" Grid.Row="4" Height="30" 
                 Margin="0,0,0,20" Padding="5" 
                 ToolTip="Введите ID пользователей через запятую (например: 2,3,5)"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Click="BtnCancel_Click" Margin="0,0,10,0" 
                    Padding="15,5" Width="100"/>
            <Button Content="Создать" Click="BtnCreate_Click" 
                    Padding="15,5" Width="100" Background="#4ecdc4"/>
        </StackPanel>
    </Grid>
</Window>